---
Theme:
  - "[[ML]]"
---

### **📍 Входные данные:**

- **Корпус текстов**: `[]string`
- **Количество слияний (мерджей)**: `N` — например, 30,000 для словаря на 30k токенов

---

### **📌 Шаг 1: Подготовка**

#### 1.1. **Разбиваем строки на "байтовые символы"**

- Каждый текст превращается в последовательность символов.    
- Каждому символу добавляем разделитель, например:  
    `"hello"` → `["h", "e", "l", "l", "o"]`


> GPT-2 использует **byte-level encoding**, где `"ñ"` и т.п. остаются уникальными байтами (UTF-8 байты). Но мы можем начать с Unicode-символов (для наглядности), потом переключиться на byte-level.

---

### **📌 Шаг 2: Построение начального словаря**

Создаём словарь `token → frequency`:

- `"h e l l o"` → `["h", "e", "l", "l", "o"]`
- `"hello"` → `["h", "e", "l", "l", "o"]`
- `"hell"` → `["h", "e", "l", "l"]`

→ Из всего корпуса собираем частоты токенов и **частоты пар**:


```sh
Pairs:
("h", "e") → 2
("e", "l") → 2
("l", "l") → 3
("l", "o") → 1
```

---

### **📌 Шаг 3: Повторение merge-шагов**

Повторяем `N` раз:

#### 🔁 Для каждой итерации:

1. Найти **самую частую пару токенов**, напр. `("l", "l")`.
2. Слить её в новый токен: `"l l"` → `"ll"`
3. Обновить словарь: заменить `"l", "l"` на `"ll"` везде
4. Пересчитать пары заново

#### Пример:

```sh
до: ["h", "e", "l", "l", "o"]
→ ["h", "e", "ll", "o"]
```

---

### **📌 Шаг 4: Стоп-критерий**

- Когда слили `N` пар — останавливаемся.
- Словарь теперь содержит начальные символы + все новые токены, полученные через слияние.

---

## ✅ Результат

- `token → id` (в лексикографическом или частотном порядке)
- `id → token`
- Готовая функция `Encode(text string) []int`
- Готовая функция `Decode([]int) string`